In app/food/page.tsx and lib/data/food.ts: read both files first. Add an optional `image` (URL string) and `imageAlt` field to the relevant data type in lib/data/food.ts, populate each entry with ONE real Creative Commons image URL of Zimbabwean food/dishes from Wikimedia Commons or Unsplash (dispatch the researcher subagent to find verified URLs and 2 short factual facts with one citation each). Wire a Next.js <Image> into the food card/section, keeping the existing layout and any framer-motion animation intact. Use width/height or fill with sizes; mark next.config.js remotePatterns if the host is new. Do NOT fetch or place photos of any real named individual. Run `npx tsc --noEmit` (or `npx next lint`) and confirm no type errors or console errors before finishing.
In app/science/page.tsx and lib/data/science.ts: read both files first. Add optional `image`/`imageAlt` fields to the data type, populate with ONE real Wikimedia/Unsplash URL per entry depicting Zimbabwean science/technology/landscape subjects (researcher subagent finds verified URLs + 2 cited facts). Wire Next.js <Image> into the section, preserving existing animations and layout. Update next.config.js remotePatterns if needed. No photos of real named individuals. Verify with build/lint, no console errors, then finish.
In app/sports/page.tsx and lib/data/sports.ts: read both files first. Add optional `image`/`imageAlt` fields, populate with ONE real Wikimedia/Unsplash URL per entry of Zimbabwean sports scenes/venues/teams-in-action (researcher subagent: verified URLs + 2 cited facts). Prefer non-portrait, crowd/venue/action imagery; do NOT place photos identifying specific named athletes. Wire Next.js <Image>, keep animations/layout. Update remotePatterns if needed. Verify build/lint clean, then finish.
In app/news/page.tsx and lib/data/news.ts: read both files first. Add optional `image`/`imageAlt` fields, populate with ONE real Wikimedia/Unsplash URL per news item using generic topical/landscape imagery related to the story subject (researcher subagent: verified URLs + confirm each headline against one citation). Do NOT fabricate quotes and do NOT attach photos to real named people. Wire Next.js <Image>, preserve layout/animation. Update remotePatterns if needed. Verify build/lint, then finish.
In app/leadership/page.tsx and lib/data/leadership.ts: read both files first. This section likely names real public figures. Do NOT fetch or place any photos of real named individuals. Instead: dispatch the researcher subagent to verify each factual claim against one reputable citation (gov/encyclopedia/news), correct any inaccuracies in lib/data/leadership.ts, and add a `sourceUrl` field per entry. Leave the existing gradient/initial-letter avatars as-is. Make no image changes. Verify build/lint clean, then finish.
In app/people/page.tsx and lib/data/people.ts: read both files first. If entries are real named individuals, do NOT fetch photos of them — instead verify facts via the researcher subagent and add a `sourceUrl` per entry, leaving avatar placeholders intact. Only if a card represents a generic/anonymous persona (e.g. "Student, Mutare") may you add a generic non-identifying landscape/cultural <Image>. Add `image`/`imageAlt` fields only where used. Preserve animations/layout, update remotePatterns if needed. Verify build/lint, then finish.
In app/improvements/page.tsx and lib/data/improvements.ts: read both files first. Add optional `image`/`imageAlt` fields, populate with ONE real Wikimedia/Unsplash URL per entry showing relevant Zimbabwean infrastructure/development/landscape subjects (researcher subagent: verified URLs + 2 cited facts). No photos of real named individuals. Wire Next.js <Image>, preserve layout/animation, update remotePatterns if needed. Verify build/lint clean, then finish.
In app/pride/page.tsx and lib/data/pride.ts: read both files first. The Notable Voices section names REAL individuals (Jordan Chanetsa, Ricky Nathanson, Raymond Sibanda) and StoryCard uses anonymous personas — do NOT fetch or place photos of the named individuals; keep their gradient avatars. For these, use the researcher subagent only to verify each biographical/legal claim against one reputable citation (e.g. Human Rights Watch, court records) and add a `sourceUrl` field. Separately, you MAY add ONE tasteful low-opacity Zimbabwean landscape/flag-neutral background <Image> behind the PrideHero ONLY IF it does not disturb the existing animated rainbow stripes and gradient (researcher finds a Creative Commons URL). Add image fields only where used; update remotePatterns if needed. Verify build/lint clean, no console errors, then finish.
